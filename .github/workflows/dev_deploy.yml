name: PLANTORY DEV CI/CD

on:
  push:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17  # Java 개발 킷 설정
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Make application.yml  # application.yml 파일 생성
        run: |
          mkdir -p src/main/resources
          cd ./src/main/resources
          echo "${{ secrets.APPLICATION_YML }}" > ./application.yml
        shell: bash

      - name: Grant execute permission for gradlew  # gradlew 실행 권한 부여
        run: chmod +x gradlew

      - name: Build Spring Boot App
        run: ./gradlew clean build -x test

#      - name: Build with Gradle  # Gradle을 사용하여 프로젝트 빌드
#        uses: gradle/gradle-build-action@v2
#        with:
#          arguments: build
#
#      - name: Upload build artifact  # 빌드된 아티팩트 업로드
#        uses: actions/upload-artifact@v4
#        with:
#          name: plantoryDev
#          path: build/libs/*.jar

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/plantory:latest

  deploy:
    needs: build  # build 작업이 성공적으로 완료된 후 실행
    runs-on: ubuntu-latest

    steps:
#      - name: Download build artifact  # 이전 단계에서 업로드한 아티팩트 다운로드
#        uses: actions/download-artifact@v4
#        with:
#          name: plantoryDev
#          path: build/libs/

      - name: Deploy to EC2  # EC2에 배포
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "💡 컨테이너 중지 및 삭제 시도 중..."
            CONTAINER_ID=$(docker ps -q --filter "name=plantory-app")

            if [ -n "$CONTAINER_ID" ]; then
              echo "🔴 기존 컨테이너를 중지하고 제거합니다."
              docker stop $CONTAINER_ID
              docker rm $CONTAINER_ID
            else
              echo "🟢 중지할 기존 컨테이너 없음."
            fi

            echo "📦 최신 Docker 이미지 pull 중..."
            docker pull plantory/plantory-image:latest

            echo "🚀 새 컨테이너 실행 중..."
            docker run -dp 8080:8080 \
              --name plantory-app \
              -e JAVA_TOOL_OPTIONS=-Duser.timezone=Asia/Seoul \
              plantory/plantory-image:latest
#        env:
#          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
#          EC2_USERNAME: ${{ secrets.EC2_USERNAME }}
#          EC2_HOST: ${{ secrets.EC2_HOST }}
#        run: |
#          echo "$EC2_SSH_KEY" > private_key.pem
#          chmod 600 private_key.pem
#          jar_file=$(find build/libs -name '*.jar' ! -name '*plain.jar' | head -n 1)
#          scp -i private_key.pem -o StrictHostKeyChecking=no "$jar_file" $EC2_USERNAME@$EC2_HOST:/home/$EC2_USERNAME/plantoryDev.jar
#
#          ssh -i private_key.pem -o StrictHostKeyChecking=no $EC2_USERNAME@$EC2_HOST << EOF
#            pkill -f 'java -jar' || true
#            sleep 5
#            nohup java -jar /home/$EC2_USERNAME/plantoryDev.jar > /home/$EC2_USERNAME/app.log 2>&1 &
#            exit
#           EOF
#
#          rm -f private_key.pem
